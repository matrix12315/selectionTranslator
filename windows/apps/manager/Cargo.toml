[package]
name = "selection-translate-manager"
version = "0.1.0"
edition = "2021"

[dependencies]
selection-core = { path = "../../../crates/core" }
selection-platform-windows = { path = "../../crates/platform-windows" }
selection-storage = { path = "../../../crates/storage" }

[target.'cfg(windows)'.dependencies]
windows = { version = "0.61", features = [
    "Win32_Foundation",
    "Win32_Graphics_Gdi",
    "Win32_System_LibraryLoader",
    "Win32_System_DataExchange",
    "Win32_System_Memory",
    "Win32_System_Ole",
    "Win32_UI_Controls",
    "Win32_UI_WindowsAndMessaging",
] }
