[package]
name = "selection-storage"
version = "0.1.0"
edition = "2021"

[dependencies]
selection-core = { path = "../core" }
selection-platform-interface = { path = "../platform-interface" }
rusqlite = { version = "0.32", features = ["bundled"] }
